<!DOCTYPE html>
<html>
<body>
    <style>
        body {
            background-color: black; color: teal; font-family: Times New 'Times New Roman', Times, serif; font-size: 30px;
        }
    </style>
    <script>

        var age = prompt("How old are you now?: ");
        var maxAge = prompt("What is the maximum age you want to reach?: ");
        var amountPerDay = prompt("How many times a day do you eat your favorite snack?: ");
        var sure = window.confirm("Your age is: " + age + ", You want to live until: " + maxAge + ", You want to eat your favorite snack " + amountPerDay + " times a day. Are you sure?");
            if(sure) {
                var totalRequired = (amountPerDay * 365) * (maxAge - age);
                document.write("You will need " + totalRequired + " of your favorite snack to last you until the ripe old age of " + maxAge + ".");
            } else {
                document.write("What do you want me to do then huh?");
            }
 
 
    </script>
</body>
</html>

<!DOCTYPE html>
<html>
<body>

<h1>The input element</h1>

<form action="/action_page.php">
  <label for="age">What is your current age?:</label>
  <input type="text" id="age" name="age"><br><br>
  <label for="xpectAge">Until what age do you want to live?:</label>
  <input type="text" id="xpectAge" name="xpectAge"><br><br>
  <label for="timesPerDay">How many times per day do you want to eat your favorite snack?:</label>
  <input type="text" id="timesPerDay" name="timesPerDay"><br><br>
</form>

<p>Click the "Submit" button and the form-data will be sent to a page on the 
server called "action_page.php".</p>

</body>
</html>